# Latent Failure Forecasting PoC v2 — Results Summary

_Auto-generated by `analysis/summarize.py` from `results/probe_results.json`._

## Verdict (v2 primary metrics)

| Metric | Value |
|---|---|
| Model | `meta-llama/Llama-3.2-3B-Instruct` |
| Trajectories (mixed tasks) | 15 (success=8, fail=7) |
| Best within-task micro-AUC | 1.0 (layer 0) |
| Best LOTO AUC | 1.0 |
| Early→late within-task delta | -0.013 (supplementary only) |
| **Decision band** | **VALIDATED (v2)** |

## Validation conditions

| Condition | Result |
|---|---|
| Within-task AUC > 0.55 | PASS |
| LOTO AUC > 0.55 | PASS |
| Shuffle drop >= 0.15 | PASS |
| Activation > text + 0.05 | FAIL |

## Within-task and LOTO AUC by relative position

| Relative position | Within-task micro-AUC | LOTO AUC |
|---|---|---|
| early | 1.000 | 1.000 |
| mid | 0.982 | 1.000 |
| late | 0.987 | 1.000 |

Chance baseline for balanced within-task classification = **0.5**. Do **not** interpret
global accuracy or global AUC (confounded by task identity in v1 design).

See `within_task_heatmap.png`, `loto_heatmap.png`, `within_task_by_position.png`.

## Recommendation

v2 controls largely pass: within-task and/or LOTO AUC above chance, shuffle ablation and text baseline support internal trajectory signal. Proceed with scaled-up collection.

## Skepticism checks

Reference: early bin, layer 0, n=15 trajectories, 3 distinct tasks, 3 with mixed labels.

## Results

| Check | Metric | Value |
|---|---|---|
| Activation probe (within-task) | micro-AUC | 1.0 (macro 1.0) |
| LOTO (leave-one-task-out) | AUC | 1.0 |
| Early text TF-IDF (within-task) | micro-AUC | n/a |
| Early text TF-IDF stripped (within-task) | micro-AUC | n/a |
| Within-task label shuffle null | p-value | 0.0033333333333333335 (null mean 0.5257142857142857) |
| Within-task shuffle (single) | AUC | 0.5357142857142858 (drop +0.464) |
| Bootstrap 95% CI (within-task AUC) | range | [1.000, 1.000] |
| Global shuffle (deprecated) | p-value | 0.0 — confounded, do not interpret |

## Verdict

v2 controls favorable: within-task AUC above chance, LOTO generalizes, per-task shuffle collapses or permutation significant.

## How to read (v2)

- **Within-task AUC (PRIMARY)**: predicts success vs failure among trajectories of the *same* task. Must be > 0.5 for latent forecasting signal.
- **LOTO**: train on all tasks except one, test on held-out task. Kills task memorization across tasks.
- **Within-task shuffle**: permutes labels within each task only. AUC → ~0.5 if clean; high AUC after shuffle = leakage.
- **Text baseline (within-task)**: if activation ≈ text, signal is surface wording not internal state.

## Methodological notes (v2)

1. **Multi-seed per task.** Each task has K trajectories (different seeds / sampling).
   Labels vary within task so probes cannot shortcut via task identity alone.
2. **Mixed-task filter.** Tasks with only successes or only failures are dropped.
3. **Primary metrics:** within-task micro-AUC and LOTO AUC. Global stratified CV is
   deprecated (`metric_tier: deprecated_confounded`).
4. **PoC scope.** Logistic probes on residual stream; correlational not causal.

## Reproduce

```bash
python run_pipeline.py
VERITAS_SMOKE_N=2 python run_pipeline.py   # smoke: 2 tasks x 3 seeds
```
