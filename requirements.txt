# Colab dependency strategy (read before editing):
#
# Do NOT list `torch` here — Colab pre-installs a CUDA build. The notebook install
# cell pins torch via a pip constraints file and force-reinstalls it after pip,
# because installing TL/transformers can mix torch subpackages (compiler vs
# _dynamo) and crash transformers import with disable(reason=).
#
# Do NOT unpin transformer_lens / transformers together blindly. TL >= 3.4 is
# required for n_ctx=8192; transformers >= 5.4 is required by TL 3.4. Those two
# must match each other AND Colab's torch — pin them, leave torch alone.
#
# sklearn / matplotlib / numpy / scipy / datasets are safe to leave loose.

transformer_lens>=3.4.0
transformers>=5.4.0

datasets
scipy
scikit-learn
matplotlib
numpy
