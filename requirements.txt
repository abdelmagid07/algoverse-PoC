# Colab dependency strategy (read before editing):
#
# Do NOT list `torch` here. Colab pre-installs a CUDA build (usually 2.7+).
# Putting bare `torch` in requirements makes pip reinstall/downgrade it, which
# breaks transformers 5.x (needs torch.compiler.disable(reason=), added in 2.7).
#
# Do NOT unpin transformer_lens / transformers together blindly. TL >= 3.4 is
# required for n_ctx=8192; transformers >= 5.4 is required by TL 3.4. Those two
# must match each other AND Colab's torch — pin them, leave torch alone.
#
# sklearn / matplotlib / numpy / scipy / datasets are safe to leave loose.

transformer_lens>=3.4.0
transformers>=5.4.0

datasets
scipy
scikit-learn
matplotlib
numpy
