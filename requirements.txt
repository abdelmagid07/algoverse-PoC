# transformer_lens >= 3.4 is required for the n_ctx override in from_pretrained
# (HookedTransformer.from_pretrained(..., n_ctx=8192)); older versions leak the
# kwarg to HuggingFace and crash. TL 3.4 in turn pins torch>=2.6 (Colab ships
# 2.7) and transformers>=5.4. With torch 2.7, torch.compiler.disable(reason=...)
# works, so the earlier transformers upper bound is no longer needed.
transformer_lens
torch
transformers
# NOTE: TL 3.4 pulls torchvision in transitively, but only imports it inside a
# try/except for vision processors we never use. On Colab a pip-installed
# torchvision often mismatches the cu126 torch build, and then transformers'
# lazy vision import crashes with "operator torchvision::nms does not exist".
# The Colab notebook therefore UNINSTALLS torchvision after install (TL tolerates
# its absence, transformers skips vision). So it is intentionally NOT pinned here.
datasets
scipy
scikit-learn
matplotlib
numpy
